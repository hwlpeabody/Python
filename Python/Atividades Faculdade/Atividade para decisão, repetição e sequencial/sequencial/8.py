#perguntar quanto ganha por hora e quantaas horas a pessoa trabalhou no mes

hora = float(input('Valor por hora: '))
trabalhadas = float(input('Horas trabalhadas por mês: '))
salario = float(hora * trabalhadas)

print(salario)